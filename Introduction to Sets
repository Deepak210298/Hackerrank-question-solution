m = int(input())
M = set(input().split())
n = int(input())
N = set(input().split())
    
for k in M:
    if k in N:
        pass
    else:
        print(k)
for j in N:
    if j in M:
        pass
    else:
        print(j)
